# JWT Authentication Configuration for Basic Memory MCP Server
# Copy this file to .env and update the values for your auth provider

# Enable JWT authentication
FASTMCP_AUTH_ENABLED=true

# Example with Supabase (replace with your project URL)
FASTMCP_AUTH_JWKS_URI=https://your-project.supabase.co/rest/v1/auth/jwks
FASTMCP_AUTH_ISSUER=https://your-project.supabase.co/auth/v1
FASTMCP_AUTH_AUDIENCE=basic-memory-mcp

# Example with Auth0 (uncomment and replace with your domain)
# FASTMCP_AUTH_JWKS_URI=https://your-domain.auth0.com/.well-known/jwks.json
# FASTMCP_AUTH_ISSUER=https://your-domain.auth0.com/
# FASTMCP_AUTH_AUDIENCE=basic-memory-mcp

# Tenant isolation (optional but recommended for multi-tenant deployments)
BASIC_MEMORY_TENANT_ID=test-tenant